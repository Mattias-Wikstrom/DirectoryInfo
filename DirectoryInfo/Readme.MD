# DirectoryInfo

Program that uses **Akka.NET** to calculate the size of a folder quickly using many CPUs instead of just one.

---

## Instructions

Click the **'Analyze Folder...'** button and select a folder whose size you are interested in (pick a reasonably sized folder, not the entire hard drive).
